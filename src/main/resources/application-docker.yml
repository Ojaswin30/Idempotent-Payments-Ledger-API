spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/payments_db
    username: postgres
    password: postgres
  data:
    redis:
      host: redis
      port: 6379
  kafka:
    bootstrap-servers: kafka:9092
